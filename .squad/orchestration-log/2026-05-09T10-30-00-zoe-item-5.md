# Orchestration Log: Zoe (Item #5) — importers/ Scaffold

**Batch Timestamp:** 2026-05-09T10:30:00-07:00  
**Agent:** Zoe (Import Adapters)  
**Work Item:** #5 — importers/ Scaffold  
**Status:** ✓ Accepted; harness only, adapters pending  

---

## Deliverable

- `importers/` directory: Commander v12 CLI harness (ESM package)
- Subcommands: rss (stub), markdown (stub), with room for future adapters
- Logger: Pino ^9.5.0 (mirrored from open-social)
- Decision document in `.squad/decisions/inbox/zoe-importers-scaffold.md` (merged to decisions.md)

---

## Key Decisions

1. **CLI framework:** commander v12.1.0 (TypeScript-first, minimal overhead, industry adoption).
2. **Package type:** ESM (`"type": "module"`), aligning with Node 22 conventions. Independent from api/ (CommonJS likely).
3. **Logger:** Pino ^9.5.0 + pino-pretty ^11.3.0, duplicated per Decision 2 (no shared code between packages).
4. **CLI runners:** tsx for dev (matched open-social scripts pattern).

---

## Open Questions for Brittany (BLOCK first real adapter implementation)

### Q#1 — Authentication Strategy
- **Option A:** Token file (~/.crate/session.json) — simple, zero CLI deps, but user must re-export on token expiry.
- **Option B:** Interactive OAuth from CLI — auto-refresh, but more setup complexity; needs separate client_id.
- **Recommendation:** Start with Option A; upgrade to B once web OAuth is stable.
- **Decision needed:** Which option for v1? File location preference?

### Q#2 — Idempotency Strategy
- **Option A:** Local sidecar (.import-state.json) — fast, offline, but machine-local.
- **Option B:** Query PDS for existing records — authoritative, survives machine changes, costs one extra XRPC call.
- **Recommendation:** Use both in production; v1 Option A sufficient.
- **Decision needed:** Is machine-local state file OK for v1? Directory preference?

---

## Status

- Commander harness complete with stubs.
- Ready for first adapter once auth + idempotency decisions locked.
