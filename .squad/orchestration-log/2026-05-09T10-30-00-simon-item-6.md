# Orchestration Log: Simon (Item #6) — Lexicon Codegen Pipeline

**Batch Timestamp:** 2026-05-09T10:30:00-07:00  
**Agent:** Simon (Lexicon Designer)  
**Work Item:** #6 — Lexicon Codegen Pipeline  
**Status:** ✓ Complete  

---

## Deliverable

- `lexicons/scripts/codegen.sh` — executable bash script with one-time setup + per-consumer codegen invocations
- `lexicons/package.json` — devDependency on `@atproto/lex-cli ^0.9.7`
- Decision document in `.squad/decisions/inbox/simon-codegen-pipeline.md` (merged to decisions.md)

---

## Key Decisions

1. **One bash script, not npm scripts:** Three separate `lex gen-*` invocations (api, web, importers) in one place.
2. **lex-cli ^0.9.7:** Matched collective-social-api's constraint.
3. **Generated outputs committed:** `api/src/lexicon/`, `web/src/lexicon/`, `importers/src/lexicon/` included in git.
4. **Safe shell idioms:** `find | sort | mapfile`, absolute paths, `mkdir -p`, `cd`-based isolation.

---

## Status

- `bash -n` passes on `codegen.sh` (syntax verified).
- Ready for first run after `npm install` in lexicons/ and dependent packages scaffold in.
