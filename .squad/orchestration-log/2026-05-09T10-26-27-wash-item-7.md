# Orchestration Log: Wash — Work Item #7

**Date:** 2026-05-09T10:26:27-07:00
**Agent:** Wash (Backend / API / OAuth)
**Mode:** Sonnet (background)

## Why Chosen

Work item #7: **Root configs + git init**. Wash is the backend lead and owns the API scaffold, making her the natural choice to set root-level project configuration files. The deployment topology and OAuth scope directives (live in `.squad/decisions/inbox/` at this timestamp) directly inform her scaffold decisions.

## Work Summary

- Created `.nvmrc` (Node 22.x, matching siblings' intent to upgrade from 20.x CI)
- Created `.prettierrc` (chose `collective-social-api` config over `open-social`'s none; `semi: true, trailingComma: es5, singleQuote: true, printWidth: 80, tabWidth: 2, useTabs: false`)
- Created `.gitignore` (merged `open-social` as base; added `**/node_modules/`, `.astro/`, `.next/`, `.env.local`, `.env.*.local`; explicitly confirmed `.squad/` is NOT excluded)
- Created `package.json` (root with `node: >=22` in `engines`, delegation scripts for test, no dev servers for importers)
- Created `README.md` (placeholder)
- Initialized `.git` repo on `main` branch
- Wrote `.squad/decisions/inbox/wash-root-scaffold.md` documenting non-obvious calls

## Files Touched

- `.nvmrc`
- `.prettierrc`
- `.gitignore`
- `package.json`
- `README.md`
- `.git/` (initialized)
- `.squad/decisions/inbox/wash-root-scaffold.md`

## Outcome

**Status:** Complete

Root scaffold established. Repo is now git-initialized and ready for downstream work items (api/, web/, landing/, lexicons/, importers/). All root config files follow sibling repo patterns with intentional upgrades (Node 22, explicit engine constraint, expanded .gitignore). No blockers for Simon's lexicon work or subsequent scaffolds.

## Notes

Acknowledged deployment topology and OAuth scope constraints without modifying root config scope — those will inform api/ work item #2 (Wash again).
